node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
Pare-Alex-Thu-Jul-11-2024.ans
algorithm.txt
test-sample.ans
ANS_FILE_FORMAT.md
dist/
.vercel_output/
.vercel/
build_output/
.vercel

# Local secrets for `vercel dev` / manual QA — NEVER commit real credentials.
# `.env.example` (placeholders only) is intentionally tracked; everything else
# that could hold a real value is ignored. `vercel dev` reads `.env`/`.env.build`;
# `vercel env pull` writes `.env.local` — both must stay untracked.
.env
.env.*
!.env.example

# .ans fixtures contain PHI — never commit
fixtures/*.ans

# tesseract.js OCR language model, downloaded at runtime (large, not source)
*.traineddata
api/_ans/__tests__/smoke.mjs
api/_ans/__tests__/adapter_smoke.mjs
api/parse.ts.full
